spring:
  freemarker:
    exposeSessionAttributes: true

server:
  port: 8080
  address: 0.0.0.0

security:
  user:
    name: admin
    password: admin
    role: ADMIN
  enableCsrf: true

http:
  mappers:
    jsonPrettyPrint: true